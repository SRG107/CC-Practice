// Given no. of paths 'M' and no. of planets 'N' , find out the critical planets i.e. those planets which do not have
// alternate path to them (only have one path to reach them)

int main()
{
	int M,N,S[10000],D[10000],i,j,sum;
	cin>>M>>N
	for(i=0;i<M;i++)
	{
		cin>>S[i]>>D[i];
	}
	for(i=0;i<M;i++)
	{
		if()
	}
}
